<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DocScan" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="BottomSheetDialogTheme"
        parent="Theme.Material3.Light.BottomSheetDialog">
        <item name="android:navigationBarColor">
            @color/bg_color
        </item>
    </style>
    <style name="btnClickColor" parent="Theme.Material3.Light">
        <item name="android:color">
            @color/white
        </item>
    </style>
</resources>